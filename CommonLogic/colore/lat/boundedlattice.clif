(cl-module BoundedLattice

(cl-imports Lattice)

(cl-comment 'Bounded lattice axioms')

(cl-comment 'L5: Bottom (zero)')

(exists (zero)
	(forall (x)
		(leq zero x)
))


(cl-comment 'L6: Top (one)')

(exists (one)
	(forall (x)
		(leq x one)
))

)
