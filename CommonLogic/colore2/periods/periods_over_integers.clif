(cl-text periods_over_integers

(cl-imports periods_mixed)

(cl-comment 'ATOM')
(forall	(x)
		(exists (y)
				(and	(inclusion y x)
						(forall	(z)
								(if	(inclusion z y)
									(= x y))))))
)	
