Directory content:

Kleene monad implementation:

* MonadSyntax.thy -- contains the basic axioms of monads.
* MultimonadSyntax.thy -- extra axioms and lemmas for 
nondeterminism (i.e. the additive monad device).
* KleeneSyntax.thy  -- axioms and lemmas for Kleene monads.

Examples:

* OneStackReverse.thy -- stack reverse example with an ad-hoc
implementation without an auxiliary stack.
* TwoStackReverse.thy -- stack reverse example with a natural
implementation, i.e. with the help of an auxiliary stack.
