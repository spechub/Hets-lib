(cl-module StonianPOrthoLattice

(cl-imports POrthoLattice)

(cl-comment 'Defining the p-ortholattices satisfying the Stone identity')

(cl-comment 'S1: Stone identity')

(forall (x y)
	(= (pc (meet x y)) (join (pc x) (pc y)))
)


)
